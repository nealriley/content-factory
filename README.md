## Content Factory

Writing toolkit and agent framework to take a content idea from research through drafting. Projects live in `writing/`, each in its own folder with `drafts` for in-progress markdown, `snippets` for reusable fragments, and `material` for sources and links.
