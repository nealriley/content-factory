Tokens are the atomic units models read (input) and write (output); prompts, system messages, retrieved snippets, and formatting overhead all count toward input.
Tokenization differs across providers, so the same 100 words can tokenize differently and skew cost. Compression, casing, and structured formats (tables/JSON/markdown) change token counts.
